# Flat hoisted node_modules preserves the dependency-resolution behavior the
# extracted packages require (notably CodeMirror and Express).
shamefully-hoist=true
public-hoist-pattern[]=*
node-linker=hoisted
